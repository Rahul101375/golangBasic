package main

import "fmt";

func main(){
	fmt.Println("there are 25 keywords")
	fmt.Println("break        default      func         interface    select:'when goroutine wait on multiple communication operation then select when one of its cases can run' case:''         defer:'to delay the execution of function or statement until nearby function returns'        go           map          struct chan:'used to declared channels'         else         goto         package      switch const        fallthrough  if           range        type continue     for          import       return       var")
}